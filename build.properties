# Application properties
app.version=0.1
# Main method of the application
main.class=de.angelcode.jirabutler.webserver.ServerStart
#main.class=de.angelcode.jirabutler.runner.BennysTestRunner
# Directories generated by init-task
build.dir=build
classes.dir=${build.dir}/classes
# Directories which have to be created manually
src.dir=src
test.dir=test
lib.dir=lib
jar.dir=${build.dir}/jar
jar.class.path.dir=${jar.dir}/lib
# Documents
docs.dir=docs
javadoc.dir=${docs.dir}/javadoc
# Reports
reports.dir=reports
# JUnut
junit.dir=${reports.dir}/junit
junit.xml.dir=${junit.dir}/xml
junit.html.dir=${junit.dir}/html
# Cobertura
cob.dir=${reports.dir}/cobertura
cob.rep=${cob.dir}/cob-rep
cob.rep.html=${cob.dir}/html
cob.instr.dir=${cob.dir}/cob-instr
cobdata=${basedir}/cobertura.ser